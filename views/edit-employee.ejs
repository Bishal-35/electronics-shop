<!DOCTYPE html>
<html>
<head>
    <title>Edit Employee</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="edit-employee-container">
        <h1 class="page-title">Edit Employee</h1>
        
        <form class="edit-employee-form" action="/update-employee/<%= employee.Employee_ID %>" method="POST">
            <div>
                <label for="name">Name:</label>
                <input type="text" id="name" name="Name" value="<%= employee.Name %>" required>
            </div>
            
            <div>
                <label for="category">Employee Category:</label>
                <select id="category" name="Employee_category" required>
                    <option value="Manager" <%= employee.Employee_category === 'Manager' ? 'selected' : '' %>>Manager</option>
                    <option value="Sales" <%= employee.Employee_category === 'Sales' ? 'selected' : '' %>>Sales</option>
                    <option value="Technical" <%= employee.Employee_category === 'Technical' ? 'selected' : '' %>>Technical</option>
                    <option value="Support" <%= employee.Employee_category === 'Support' ? 'selected' : '' %>>Support</option>
                </select>
            </div>
            
            <div>
                <label for="role">Role:</label>
                <input type="text" id="role" name="Role" value="<%= employee.Role %>" required>
            </div>
            
            <div>
                <label for="status">Attendance Status:</label>
                <select id="status" name="Status">
                    <option value="Present" <%= employee.Today_Status === 'Present' ? 'selected' : '' %>>Present</option>
                    <option value="Absent" <%= employee.Today_Status === 'Absent' ? 'selected' : '' %>>Absent</option>
                    <option value="Leave" <%= employee.Today_Status === 'Leave' ? 'selected' : '' %>>Leave</option>
                </select>
            </div>
            
            <div class="button-container">
                <button type="submit" class="update-btn">Update Employee</button>
                <a href="/employees" class="cancel-btn">Cancel</a>
                <button type="button" class="delete-btn" onclick="if(confirm('Are you sure you want to delete this employee?')) window.location.href='/delete-employee/<%= employee.Employee_ID %>'">Delete Employee</button>
            </div>
        </form>
    </div>
</body>
</html>